# Django Settings
SECRET_KEY=django-insecure-hackathon-demo-key-change-later
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (we'll use SQLite for now - faster setup)
# DATABASE_URL=  # Leave commented for SQLite

# CORS Settings
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Cloudinary (we'll add these later)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Stripe (we'll add these later)
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=

# Google Maps (we'll add this later)
GOOGLE_MAPS_API_KEY=

# JWT Settings
JWT_ACCESS_TOKEN_LIFETIME=60
JWT_REFRESH_TOKEN_LIFETIME=1440

# Frontend URL
FRONTEND_URL=http://localhost:5173